Update your configuration file (config.ini) with the necessary settings.

Save the updated Python script as main.py

Install the required Python packages using:

pip install requests

Run the script:

python main.py

